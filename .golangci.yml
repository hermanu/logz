# golangci-lint configuration v2
version: "2"

linters:
  default: none
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - unused
    - bodyclose
    - errorlint
    - exhaustive
    - nilerr

run:
  timeout: 5m
  tests: true